<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Camera is the primary sensor: everything the stack perceives comes
        from it.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
        Location gives absolute speed and the route. The perception stack runs
        without it, at reduced speed confidence and with no navigation, so a
        refusal degrades the app rather than blocking it.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Recording a long drive while the screen is on. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Only the online navigation module needs the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Accelerometer and gyroscope are required; the app cannot estimate
         ego motion without them. -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!--
        PERMISSIONS REMOVED FROM THE MERGED MANIFEST.

        The CameraX plugin declares RECORD_AUDIO and WRITE_EXTERNAL_STORAGE
        because it also supports video recording. This app only ever consumes
        an image stream (`enableAudio: false`), never records video, and never
        writes to shared storage — recordings go to the app's own private
        directory. Leaving those permissions in the merged manifest would make
        the app appear to request the microphone, which for a tool whose whole
        claim is that it has no hidden capabilities would be exactly the wrong
        signal. They are removed rather than merely unused, so the permission
        list a user sees at install matches what the app can actually do.

        The plugin's READ_PHONE_STATE is likewise dropped: nothing here reads
        telephony state.

        The audio plugins add no user-facing permission at all — playback and
        text-to-speech need none. They do contribute a signature-level
        `com.aicar.aicar.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`, which
        AndroidX generates so an app's own dynamically-registered broadcast
        receivers are not exported. It is granted only to this app's own
        signature, is invisible in the install dialog, and is noted here so
        that seeing it in an `aapt dump` is not a surprise.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"
        tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"
        tools:node="remove" />

    <!--
        NOTE ON WHAT IS DELIBERATELY ABSENT.

        This application is simulation-only. It contains no Bluetooth, USB or
        serial permissions, because it has no code that talks to a vehicle:
        no CAN bus, no OBD-II, no actuator interface. The steering, throttle
        and brake values it computes are drawn on screen and written to a log
        file, and there is no path by which they reach anything else.
        See docs/SAFETY.md.
    -->

    <application
        android:label="Driving Simulator"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:largeHeap="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
